import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Wellet Connect app smoke test', (WidgetTester tester) async {
    // Placeholder test — full widget tests require Supabase and
    // platform channel mocks which are configured during CI setup.
    expect(true, isTrue);
  });
}
